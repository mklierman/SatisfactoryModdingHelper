steps:
- uses: actions/checkout@v2
- uses: actions/setup-dotnet@v1
  with:
    dotnet-version: 'net6.0.x' # SDK Version to use; x will use the latest version of the 3.1 channel
- name: Install dependencies
  run: dotnet restore
- name: Build
  run: dotnet build
